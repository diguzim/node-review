// TODO: change this to a job that sends a notification somewhere that the description has changed for a creature

export function NotifyLibraryEntryDescriptionChangeWorker(race: string) {
    console.log(`The description has changed for the following race: ${race}`);
}